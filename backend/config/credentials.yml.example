# RAILS_ENV=development rails credentials:edit
# Store secrets here. Do not commit this file.
# All keys accessible via: Rails.application.credentials.key

# rails secret -> paste here
secret_key_base: REPLACE_WITH_RAILS_SECRET

# JWT
jwt_secret: REPLACE_WITH_JWT_SECRET

# Stripe
stripe_secret_key: sk_test_REPLACE_ME
stripe_webhook_secret: whsec_REPLACE_ME
